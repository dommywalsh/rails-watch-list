<h1 class="text-center pt-4">Lists</h1>
<div class="container mt-4 ">
  <div class="cards d-grid p-2">
  <% @lists.each do |list| %>
    <div class="card-category"><%=link_to list.name, list_path(list)%>
    </div>
  <% end %>
  </div>
</div>
